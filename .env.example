# Copy to .env (gitignored) and fill in. Never commit real secrets.

# OpenRouter (OpenAI-compatible) — used for the digest call. See ADR 003.
OPENROUTER_API_KEY=

# Postgres connection string (Vercel Postgres / Neon). See ADR 001.
DATABASE_URL=
